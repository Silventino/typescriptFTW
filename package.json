{
  "name": "shared-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "start cmd.exe @cmd /k \"cd shared && yarn start\" && start cmd.exe @cmd /k \"cd backend && yarn start\" && start cmd.exe @cmd /k \"cd frontend && yarn start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT"
}
